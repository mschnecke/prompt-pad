Build and install PromptPad to /Applications.

```bash
./scripts/install.sh
```

After successful installation, tell the user: "PromptPad installed to /Applications/PromptPad.app - launch it from Spotlight or Finder."
